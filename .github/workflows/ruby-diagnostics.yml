name: PR3572 Ruby diagnostics
on:
  push:
    branches: [diagnostics/pr3572-ruby]
permissions:
  contents: read
jobs:
  build:

    # When continue-on-error is true for an individual build,
    # that build can fail (it'll show red),
    # but it won't fail the overall build
    continue-on-error: ${{ matrix.continue-on-error || false }}

    # https://github.com/actions/runner-images/tree/main/images/macos
    runs-on: ${{ matrix.os || 'macos-26' }}

    # The name of the test follow the tested language
    name: >
      ${{ matrix.label }}
      ${{ matrix.SWIGLANG || 'none' }} ${{ matrix.VER }}
      ${{ matrix.SWIG_FEATURES }}
      ${{ matrix.COMPILER || 'clang' }}
      ${{ matrix.GCC }}
      ${{ matrix.CPPSTD }}
      ${{ matrix.CSTD }}
      ${{ matrix.os }}
      ${{ matrix.continue-on-error && '(can fail)' }}

    timeout-minutes: 35

    strategy:
      matrix:
        include:
        - SWIGLANG: ruby
          source_ref: a97791eeeb139643ceb4ef9da3be320e3c6bfbf1
          label: pr3572
        - SWIGLANG: ruby
          source_ref: 0f5a59428e99ed7003d785dfea3b4381c0879c4c
          label: upstream
      # Run all of them, as opposed to aborting when one fails
      fail-fast: false

    env:
      SWIGLANG: ${{ matrix.SWIGLANG }}
      VER: ${{ matrix.VER }}
      SWIG_FEATURES: ${{ matrix.SWIG_FEATURES }}
      GCC: ${{ matrix.GCC }}
      CPPSTD: ${{ matrix.CPPSTD }}
      CSTD: ${{ matrix.CSTD }}
      HOMEBREW_REQUIRE_TAP_TRUST: 1
      HOMEBREW_NO_ENV_HINTS: 1
      HOMEBREW_NO_ASK: 1

    steps:
    - name: Machine Info
      run: |
          echo "number of logical cores logical cpu..."
          sysctl -n hw.logicalcpu
          echo "number of cores..."
          sysctl -n hw.ncpu
          echo "uname..."
          uname -a
          echo "total memory in bytes..."
          sysctl hw.memsize

    - name: Checkout
      uses: actions/checkout@v6
      with:
        ref: ${{ matrix.source_ref }}
        show-progress: false
        persist-credentials: false

    - name: Install CCache
      uses: hendrikmuhs/ccache-action@v1.2.23
      with:
        key: ${{ matrix.os || 'macos-26' }}-${{ matrix.COMPILER || 'clang' }}${{ matrix.GCC }}

    - name: Install Dependencies
      run: |
          set -x

          # Remove AWS tap
          brew untap aws/tap
          # Trust Microsoft Azure tap
          brew trust azure/bicep
          brew_pkgs=(boost bison automake)
          if test "$SWIGLANG" = "lua"; then
            brew_pkgs+=(lua)
          fi
          # See Homebrew site: https://brew.sh/
          brew install ${brew_pkgs[@]}
          # Ensure we use the newer version of bison
          bison_dir="$(brew ls bison | grep '/bin/bison$' | head -1 | sed 's?/bison$??')"
          echo "$bison_dir" >> $GITHUB_PATH
          BOOST_INC="$(brew ls boost | grep 'boost/shared_ptr.hpp$' | head -1 | sed 's?/boost/shared_ptr.hpp$??')"
          # Boost header include path, for testing
          echo "BOOST_INC=$BOOST_INC" >> $GITHUB_ENV
          if test "$SWIGLANG" = "lua"; then
            lua_dir="$(brew ls lua | grep '/bin/lua$' | head -1 | sed 's?/bin/lua$??')"
            echo "LUADIR=$lua_dir" >> $GITHUB_ENV
          fi

          cpu_count=$(sysctl -n hw.ncpu)
          if test $cpu_count -gt 1; then
            echo "SWIGJOBS=-j$cpu_count" >> $GITHUB_ENV
          fi

          # TODO: Tools/testflags.py defaults are too old
          if test -z "$CPPSTD"; then
            CPPSTD=c++14
            echo "CPPSTD=$CPPSTD" >> $GITHUB_ENV
          fi
          if test -z "$CSTD"; then
            case "$CPPSTD" in
            c++11) CSTD=c11 ;;
            c++14) CSTD=c11 ;;
            c++17) CSTD=c17 ;;
            c++20) CSTD=c17 ;;
            *)     CSTD=c11 ;; # TODO: Tools/testflags.py defaults are too old
            esac
            echo "CSTD=$CSTD" >> $GITHUB_ENV
          fi

          if test '${{ matrix.COMPILER }}' = 'gcc' && test -n "$GCC"; then
            # GCC front-end is available with versioned only!
            CC="gcc-$GCC"
            CXX="g++-$GCC"
          else # The defualt on MacOS is Clang
            CFLAGS+=" -Wno-deprecated-declarations"
            CXXFLAGS+=" -Wno-deprecated-declarations"
            CC='clang'
            CXX='clang++'
          fi
          echo "CC=$CC" >> $GITHUB_ENV
          echo "CXX=$CXX" >> $GITHUB_ENV
          ls -la $(which $CC) $(which $CXX)
          $CC --version
          $CXX --version
          $CXX -x c++ -dM -E - < /dev/null | grep __cplusplus
          $CC -dM -E - < /dev/null | grep __STDC_VERSION__

          echo "Use C++ standard $CPPSTD"
          CXXFLAGS+=" -std=$CPPSTD"
          echo "CXXFLAGS=$CXXFLAGS" >> $GITHUB_ENV

          echo "Use C standard $CSTD"
          CFLAGS+=" -std=$CSTD"
          echo "CFLAGS=$CFLAGS" >> $GITHUB_ENV

          if test -n "$SWIGLANG"; then
            WITHLANG="$SWIGLANG"
            if test "$SWIGLANG" = 'python'; then
              # We only use python3
              WITHLANG+=3
            fi
            echo "WITHLANG=$WITHLANG" >> $GITHUB_ENV
            if test -n "$VER"; then
              arc=$(arch)
              case "$SWIGLANG" in
              java)
                java_path="JAVA_HOME_${VER}_$arc"
                echo "JAVA_HOME=${!java_path}" >> $GITHUB_ENV
                ;;
              python)
                echo "/Library/Frameworks/Python.framework/Versions/$VER/bin" >> $GITHUB_PATH
                # Can also be found in:
                # "$RUNNER_TOOL_CACHE/Python/$VER.*/$arc/bin"
                ;;
              ruby)
                ruby_dir="$(ls -d $RUNNER_TOOL_CACHE/Ruby/$VER.*/$arc/bin)"
                echo "$ruby_dir" >> $GITHUB_PATH
                ;;
              go)
                go_dir="$(ls -d $RUNNER_TOOL_CACHE/go/$VER.*/$arc/bin)"
                echo "$go_dir" >> $GITHUB_PATH
                ;;
              esac
            fi
          fi

    - name: Configure
      run: |
          set -x

          if test -n "$WITHLANG"; then
            CONFIGOPTS+=(--without-alllang --with-$WITHLANG)
          fi
          CONFIGOPTS+=(CFLAGS="$CFLAGS")
          CONFIGOPTS+=(CXXFLAGS="$CXXFLAGS")
          if test -n "$LUADIR"; then
            CONFIGOPTS+=(--with-lua="$LUADIR/bin/lua" --with-luaincl="$LUADIR/include/lua" --with-lualib="$LUADIR/lib")
          fi
          echo "${CONFIGOPTS[@]}"
          ./autogen.sh && mkdir -p build/build && cd build/build && ../../configure "${CONFIGOPTS[@]}"

    - name: Build
      working-directory: build/build
      run: |
          set -x
          make -s $SWIGJOBS
          ./swig -version && ./swig -pcreversion

    - name: Library and environment diagnostics
      run: |
          mkdir -p diagnostic-artifacts
          git rev-parse HEAD | tee diagnostic-artifacts/head.txt
          git status --short | tee diagnostic-artifacts/status-before.txt
          git ls-files Lib | xargs shasum -a 256 > diagnostic-artifacts/library-before.sha256
          ruby --version
          ruby -rrbconfig -e 'p RbConfig::CONFIG.values_at("rubyhdrdir", "rubyarchhdrdir", "CC", "CXX")'
          df -h
          vm_stat
          ulimit -a
          cd build/build
          ./preinst-swig -ruby -debug-include -E ../../Examples/test-suite/li_cpointer.i > ../../diagnostic-artifacts/li_cpointer-preprocessed.txt

    - name: Test
      working-directory: build/build
      run: |
          set -x

          if test -z "$SWIGLANG"; then
            make $SWIGJOBS check-ccache
            make $SWIGJOBS check-errors-test-suite
          else
            # Stricter compile flags for examples.
            # Various headers and SWIG generated code prevents full use of -pedantic.
            cflags="$($GITHUB_WORKSPACE/Tools/testflags.py --language $SWIGLANG --cflags --std=$CSTD --compiler=$CC)"
            cxxflags="$($GITHUB_WORKSPACE/Tools/testflags.py --language $SWIGLANG --cxxflags --std=$CPPSTD --compiler=$CC)"
            # Include for the boost headers
            cxxflags+=" -I$BOOST_INC"
            make check-$SWIGLANG-version
            make check-$SWIGLANG-enabled
            make $SWIGJOBS check-$SWIGLANG-examples CFLAGS="$cflags" CXXFLAGS="$cxxflags"
            make $SWIGJOBS check-$SWIGLANG-test-suite CFLAGS="$cflags" CXXFLAGS="$cxxflags"
          fi

    - name: Collect diagnostics
      if: always()
      run: |
          mkdir -p diagnostic-artifacts
          git status --short > diagnostic-artifacts/status-after.txt
          git ls-files Lib | xargs shasum -a 256 > diagnostic-artifacts/library-after.sha256
          diff -u diagnostic-artifacts/library-before.sha256 diagnostic-artifacts/library-after.sha256 > diagnostic-artifacts/library-changes.txt || true
          cp build/build/config.log diagnostic-artifacts/ 2>/dev/null || true
          for stem in typemap_directorout li_cmalloc li_constraints li_cdata_bytes li_cpointer li_math; do
            for file in build/build/Examples/test-suite/ruby/${stem}_wrap.*; do
              if test -f "$file"; then cp "$file" diagnostic-artifacts/; fi
            done
          done
          df -h > diagnostic-artifacts/disk.txt
          vm_stat > diagnostic-artifacts/memory.txt

    - uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ruby-diagnostics-${{ matrix.label }}
        path: diagnostic-artifacts/
